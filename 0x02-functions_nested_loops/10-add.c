#include "main.h"
/**
 * add - Entry point
 * @a: first param
 * @b: second param
 * Description: Adds two ints and returns the result
 * Return: addition result
 */
int add(int a, int b)
{
	return (a + b);
}

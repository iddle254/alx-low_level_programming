#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * _abs - Entry point
 * @n: number to calculate
 * Description: computes the absolute value of an integer
 * Return: absolutevalue
 */
int _abs(int n)
{
	return abs(n);
}
